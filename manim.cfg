[CLI]
# No background_color here: lib/palette.py sets it from the active theme at import, and every scene imports it.
# Caching off, as docs/manim.md requires: a cached animation hides a change you just made. bin/render.sh passes it too.
disable_caching = True
media_dir = ./media
